# Docker tasks for navigator

[tasks."build:factory"]
description = "Build the factory Docker image"
dir = "{{config_root}}/.."
run = "docker build -f docker/Dockerfile.factory -t navigator-factory ."

[tasks."build:all"]
description = "Build all Docker images"
depends = ["build:factory"]
